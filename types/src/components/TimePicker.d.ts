/// <reference types="react" />
import { TimePickerProps } from '../type/TimePicker.type';
export default function TimePicker({ timeslotArray, calendarStyles, disableTimeslot, timeZone, onClickSelectedTimeslot, isTimeslot, selectedDate, selectedTimeslot }: TimePickerProps): JSX.Element;
